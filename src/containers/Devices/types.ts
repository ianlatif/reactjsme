export interface StatInfoProps {
  image: React.ReactChild;
  name: string;
  value: number | string;
  bgColor?: string;
}

export interface StatInfoWrapperProps {
  bgColor?: string;
}

export interface BarChartProps {
  title: string;
}
